[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "robobench"
version = "0.1.0"
description = "A harness for benchmarking new visuomotor architectures on LIBERO"
requires-python = ">=3.9"

[tool.setuptools.packages.find]
where = ["src"]
