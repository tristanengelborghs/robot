[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "dexhand"
version = "0.1.0"
description = "In-hand cube reorientation with dexterous hands: MuJoCo env, PPO teacher, DAgger student"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
